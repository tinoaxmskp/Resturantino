package com.example.resturantino.utils

// Placeholder Firebase utils object.
// Keep it empty for now so you don't get compile errors.
// Later you can add methods like getFirestore(), getAuth(), etc.
object FirebaseUtils {
    // Example:
    // fun getAuth() = FirebaseAuth.getInstance()
    // fun getFirestore() = FirebaseFirestore.getInstance()
}
